apiVersion: apps/v1
kind: Deployment
metadata:
  name: compliance-engine
  namespace: stellar-pay
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: compliance-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: compliance-engine
    spec:
      containers:
        - name: compliance-engine
          image: ghcr.io/missblue00/stellar-pay-compliance-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3003
          envFrom:
            - configMapRef:
                name: stellar-pay-config
            - secretRef:
                name: stellar-pay-secrets
          env:
            - name: PORT
              value: '3003'
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: compliance-engine
  namespace: stellar-pay
spec:
  selector:
    app.kubernetes.io/name: compliance-engine
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: compliance-engine
  namespace: stellar-pay
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: compliance-engine
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
