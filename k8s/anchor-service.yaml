apiVersion: apps/v1
kind: Deployment
metadata:
  name: anchor-service
  namespace: stellar-pay
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: anchor-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: anchor-service
    spec:
      containers:
        - name: anchor-service
          image: ghcr.io/missblue00/stellar-pay-anchor-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3002
          envFrom:
            - configMapRef:
                name: stellar-pay-config
            - secretRef:
                name: stellar-pay-secrets
          env:
            - name: PORT
              value: '3002'
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: anchor-service
  namespace: stellar-pay
spec:
  selector:
    app.kubernetes.io/name: anchor-service
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: anchor-service
  namespace: stellar-pay
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: anchor-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
